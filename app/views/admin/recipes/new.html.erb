<div id="recipes">
 <% @recipes.each do |recipe| %>
   <button v-on:click="setRecipeInfo(<%= recipe.id %>)"><%= recipe.id %></button>
 <% end %>
 <div>
   <p>ingredients: {{ recipeInfo }}</p>
 </div>
</div>
<%= javascript_pack_tag 'hello_vue' %>

<style>
	button {
		font-size: 20px;
	}
</style>